diff --git a/server/graph_service/zep_graphiti.py b/server/graph_service/zep_graphiti.py
index 8ca65ae..711666d 100644
--- a/server/graph_service/zep_graphiti.py
+++ b/server/graph_service/zep_graphiti.py
@@ -5,8 +5,11 @@ from typing import Annotated
 from fastapi import Depends, HTTPException
 from graphiti_core import Graphiti  # type: ignore
+from graphiti_core.embedder.gemini import GeminiEmbedder, GeminiEmbedderConfig
 from graphiti_core.edges import EntityEdge  # type: ignore
 from graphiti_core.errors import EdgeNotFoundError, GroupsEdgesNotFoundError, NodeNotFoundError
 from graphiti_core.llm_client import LLMClient  # type: ignore
+from graphiti_core.llm_client.config import LLMConfig
+from graphiti_core.llm_client.gemini_client import GeminiClient
 from graphiti_core.nodes import EntityNode, EpisodicNode  # type: ignore
 
 from graph_service.config import ZepEnvDep
@@ -103,10 +106,26 @@ def _create_graphiti_client(settings: ZepEnvDep) -> ZepGraphiti:
                 'Neo4j configuration (neo4j_uri, neo4j_user, neo4j_password) is required '
                 "when db_backend is 'neo4j'"
             )
+        llm_client = GeminiClient(
+            LLMConfig(
+                api_key=settings.openai_api_key,
+                model=settings.model_name,
+                small_model=settings.model_name,
+            )
+        )
+        embedder = GeminiEmbedder(
+            GeminiEmbedderConfig(
+                api_key=settings.openai_api_key,
+                embedding_model=settings.embedding_model_name,
+                embedding_dim=3072,
+            )
+        )
         return ZepGraphiti(
             uri=settings.neo4j_uri,
             user=settings.neo4j_user,
             password=settings.neo4j_password,
+            llm_client=llm_client,
+            embedder=embedder,
         )
 
 
@@ -111,6 +128,2 @@ def _configure_graphiti_client(client: ZepGraphiti, settings: ZepEnvDep) -> None:
-    if settings.openai_base_url is not None:
-        client.llm_client.config.base_url = settings.openai_base_url
-    if settings.openai_api_key is not None:
-        client.llm_client.config.api_key = settings.openai_api_key
     if settings.model_name is not None:
         client.llm_client.model = settings.model_name
diff --git a/server/pyproject.toml b/server/pyproject.toml
index dc5567d..4d2b134 100644
--- a/server/pyproject.toml
+++ b/server/pyproject.toml
@@ -9,6 +9,7 @@ requires-python = ">=3.10"
 dependencies = [
     "fastapi>=0.115.0",
-    "graphiti-core>=0.28.2",
+    "graphiti-core[google-genai]==0.28.2",
+    "google-genai==1.62.0",
     "pydantic-settings>=2.4.0",
     "uvicorn>=0.44.0",
     "httpx>=0.28.1",
